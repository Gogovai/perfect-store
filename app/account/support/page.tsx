'use client';import{useEffect,useState}from'react';import{createClient}from'@/lib/supabase/client';import{createTicket}from'./actions';
export default function SupportPage(){const[rows,setRows]=useState<any[]>([]);const[form,setForm]=useState({subject:'',category:'general',priority:'normal',orderId:'',message:''});const[msg,setMsg]=useState('');const load=async()=>{const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user){location.href='/login';return}const{data}=await s.from('support_tickets').select('id,subject,category,priority,status,created_at,order_id').eq('customer_id',user.id).order('created_at',{ascending:false});setRows(data||[])};useEffect(()=>{load()},[]);async function submit(e:React.FormEvent){e.preventDefault();const r=await createTicket(form);setMsg(r.success?'Ticket created':r.error||'Unable to create');if(r.success){setForm({subject:'',category:'general',priority:'normal',orderId:'',message:''});load()}}return <main className="min-h-screen bg-gray-50 py-8"><div className="mx-auto max-w-5xl space-y-6 px-4"><div><h1 className="text-3xl font-bold">Support</h1><p className="mt-1 text-sm text-gray-500">Get help with orders, payments, delivery, returns or your account.</p></div><form onSubmit={submit} className="rounded-2xl border bg-white p-6 space-y-4"><div className="grid gap-4 sm:grid-cols-2"><input required placeholder="Subject" value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})} className="rounded-xl border px-3 py-3"/><select value={form.category} onChange={e=>setForm({...form,category:e.target.value})} className="rounded-xl border px-3 py-3"><option value="general">General</option><option value="order">Order</option><option value="payment">Payment</option><option value="delivery">Delivery</option><option value="return">Return</option><option value="account">Account</option></select><select value={form.priority} onChange={e=>setForm({...form,priority:e.target.value})} className="rounded-xl border px-3 py-3"><option value="low">Low</option><option value="normal">Normal</option><option value="high">High</option><option value="urgent">Urgent</option></select><input placeholder="Order ID (optional)" value={form.orderId} onChange={e=>setForm({...form,orderId:e.target.value})} className="rounded-xl border px-3 py-3"/></div><textarea required rows={5} placeholder="Describe the issue" value={form.message} onChange={e=>setForm({...form,message:e.target.value})} className="w-full rounded-xl border px-3 py-3"/>{msg&&<p className="text-sm text-gray-600">{msg}</p>}<button className="rounded-xl bg-[#0f2b5b] px-5 py-3 font-semibold text-white">Create ticket</button></form><section className="rounded-2xl border bg-white"><div className="border-b p-5 font-semibold">Your tickets</div>{rows.length===0?<p className="p-6 text-sm text-gray-500">No tickets yet.</p>:rows.map(r=><div key={r.id} className="flex flex-wrap items-center justify-between gap-3 border-b p-5 last:border-0"><div><p className="font-semibold">{r.subject}</p><p className="text-xs text-gray-500">{r.category} · {new Date(r.created_at).toLocaleDateString()}</p></div><span className="rounded-full bg-gray-100 px-3 py-1 text-xs font-semibold capitalize">{r.status}</span></div>)}</section></div></main>}
