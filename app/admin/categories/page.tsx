'use client';import{useEffect,useState}from'react';import{createClient}from'@/lib/supabase/client';import{createCategory,toggleCategory}from'./actions';export default function AdminCategories(){const[cats,setCats]=useState<any[]>([]);const[f,setF]=useState({name:'',slug:'',description:'',imageUrl:'',sortOrder:'0'});const[msg,setMsg]=useState('');async function load(){const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user){location.href='/login';return}const{data:p}=await s.from('profiles').select('role').eq('id',user.id).single();if(p?.role!=='admin'){location.href='/';return}const{data}=await s.from('categories').select('*').order('sort_order');setCats(data||[])}useEffect(()=>{load()},[]);return <main className="min-h-screen bg-gray-50 p-4 sm:p-8"><div className="mx-auto max-w-5xl space-y-6"><h1 className="text-3xl font-bold">Categories</h1><form onSubmit={async e=>{e.preventDefault();const r=await createCategory(f);setMsg(r.success?'Category created':r.error||'Unable to create');if(r.success){setF({name:'',slug:'',description:'',imageUrl:'',sortOrder:'0'});load()}}} className="rounded-2xl border bg-white p-6"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3"><input required placeholder="Name" value={f.name} onChange={e=>setF({...f,name:e.target.value})} className="rounded-lg border px-3 py-2"/><input required placeholder="Slug" value={f.slug} onChange={e=>setF({...f,slug:e.target.value})} className="rounded-lg border px-3 py-2"/><input placeholder="Description" value={f.description} onChange={e=>setF({...f,description:e.target.value})} className="rounded-lg border px-3 py-2"/><input type="url" placeholder="Image URL" value={f.imageUrl} onChange={e=>setF({...f,imageUrl:e.target.value})} className="rounded-lg border px-3 py-2"/><input type="number" min="0" placeholder="Sort order" value={f.sortOrder} onChange={e=>setF({...f,sortOrder:e.target.value})} className="rounded-lg border px-3 py-2"/></div>{msg&&<p className="mt-3 text-sm text-gray-600">{msg}</p>}<button className="mt-4 rounded-lg bg-[#0f2b5b] px-5 py-2.5 text-sm font-semibold text-white">Create category</button></form><div className="divide-y rounded-2xl border bg-white">{cats.map(c=><div key={c.id} className="flex items-center justify-between p-5"><div><p className="font-medium">{c.name}</p><p className="text-xs text-gray-500">{c.slug}</p></div><button onClick={async()=>{await toggleCategory(c.id,!c.is_active);load()}} className="rounded-lg border px-3 py-2 text-xs">{c.is_active?'Disable':'Enable'}</button></div>)}</div></div></main>}
