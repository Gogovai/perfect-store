'use client';
import {useEffect,useState} from 'react';import {createClient} from '@/lib/supabase/client';import {createCoupon,toggleCoupon} from './actions';
export default function CouponsPage(){const[coupons,setCoupons]=useState<any[]>([]);const[form,setForm]=useState({code:'',description:'',discountType:'percentage',discountValue:'',minimumOrderAmount:'0',maximumDiscountAmount:'',usageLimit:'',expiresAt:''});const[msg,setMsg]=useState('');async function load(){const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user){location.href='/login';return}const{data:p}=await s.from('profiles').select('role').eq('id',user.id).single();if((p as any)?.role!=='admin'){location.href='/';return}const{data}=await s.from('coupons').select('*').order('created_at',{ascending:false});setCoupons(data||[])}useEffect(()=>{load()},[]);async function submit(e:React.FormEvent){e.preventDefault();const r=await createCoupon(form);setMsg(r.success?'Coupon created':r.error||'Unable to create coupon');if(r.success){setForm({code:'',description:'',discountType:'percentage',discountValue:'',minimumOrderAmount:'0',maximumDiscountAmount:'',usageLimit:'',expiresAt:''});load()}}return <main className="min-h-screen bg-gray-50 p-4 sm:p-8"><div className="mx-auto max-w-5xl space-y-6"><h1 className="text-3xl font-bold">Coupons</h1><form onSubmit={submit} className="rounded-2xl border bg-white p-6"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3"><input required placeholder="Code" value={form.code} onChange={e=>setForm({...form,code:e.target.value})} className="rounded-lg border px-3 py-2"/><input placeholder="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="rounded-lg border px-3 py-2"/><select value={form.discountType} onChange={e=>setForm({...form,discountType:e.target.value})} className="rounded-lg border px-3 py-2"><option value="percentage">Percentage</option><option value="fixed">Fixed GHS</option></select><input required type="number" min="0.01" step="0.01" placeholder="Discount value" value={form.discountValue} onChange={e=>setForm({...form,discountValue:e.target.value})} className="rounded-lg border px-3 py-2"/><input type="number" min="0" step="0.01" placeholder="Minimum order" value={form.minimumOrderAmount} onChange={e=>setForm({...form,minimumOrderAmount:e.target.value})} className="rounded-lg border px-3 py-2"/><input type="number" min="0" step="0.01" placeholder="Maximum discount" value={form.maximumDiscountAmount} onChange={e=>setForm({...form,maximumDiscountAmount:e.target.value})} className="rounded-lg border px-3 py-2"/><input type="number" min="1" placeholder="Usage limit" value={form.usageLimit} onChange={e=>setForm({...form,usageLimit:e.target.value})} className="rounded-lg border px-3 py-2"/><input type="datetime-local" value={form.expiresAt} onChange={e=>setForm({...form,expiresAt:e.target.value})} className="rounded-lg border px-3 py-2"/></div>{msg&&<p className="mt-3 text-sm text-gray-600">{msg}</p>}<button className="mt-4 rounded-lg bg-[#0f2b5b] px-5 py-2.5 text-sm font-semibold text-white">Create coupon</button></form><div className="divide-y rounded-2xl border bg-white">{coupons.map(c=><div key={c.id} className="flex items-center justify-between gap-4 p-5"><div><p className="font-semibold">{c.code}</p><p className="text-sm text-gray-500">{c.discount_type==='percentage'?`${c.discount_value}%`:`GHS ${c.discount_value}`} · used {c.used_count}{c.usage_limit?`/${c.usage_limit}`:''}</p></div><button onClick={async()=>{await toggleCoupon(c.id,!c.is_active);load()}} className="rounded-lg border px-3 py-2 text-xs font-semibold">{c.is_active?'Disable':'Enable'}</button></div>)}</div></div></main>}
