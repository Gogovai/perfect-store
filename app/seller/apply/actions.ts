'use server';
import { createClient } from '@/lib/supabase/server';
import { z } from 'zod';
const schema=z.object({storeName:z.string().trim().min(2).max(80),description:z.string().trim().max(500).optional(),phone:z.string().trim().min(7).max(30),email:z.string().trim().email().max(160),legalBusinessName:z.string().trim().max(160).optional(),businessType:z.string().trim().max(80).optional(),registrationNumber:z.string().trim().max(100).optional(),taxId:z.string().trim().max(100).optional(),contactName:z.string().trim().max(120).optional(),addressLine1:z.string().trim().max(200).optional(),city:z.string().trim().max(80).optional(),region:z.string().trim().max(80).optional()});
export async function submitSellerApplication(input:unknown){const parsed=schema.safeParse(input);if(!parsed.success)return{success:false,error:parsed.error.issues[0]?.message??'Invalid application'};const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return{success:false,error:'Please log in before applying.'};const{data,error}=await(supabase as any).rpc('submit_seller_application',{p_store_name:parsed.data.storeName,p_description:parsed.data.description??null,p_phone:parsed.data.phone,p_email:parsed.data.email});if(error)return{success:false,error:error.message||'Unable to submit application.'};const sellerId=data?.seller_id as string|undefined;if(sellerId){const{error:businessError}=await supabase.from('seller_business_profiles').upsert({seller_id:sellerId,legal_business_name:parsed.data.legalBusinessName||null,business_type:parsed.data.businessType||null,registration_number:parsed.data.registrationNumber||null,tax_id:parsed.data.taxId||null,contact_name:parsed.data.contactName||null,contact_phone:parsed.data.phone,contact_email:parsed.data.email,address_line1:parsed.data.addressLine1||null,city:parsed.data.city||null,region:parsed.data.region||null,country:'Ghana'},{onConflict:'seller_id'});if(businessError)return{success:false,error:'Application submitted, but business details could not be saved. Please contact support.'};}return{success:true,sellerId};}
export async function getSellerApplication(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return null;const{data}=await supabase.from('sellers').select('id,store_name,description,phone,email,status,created_at').eq('owner_id',user.id).maybeSingle();return data;}
