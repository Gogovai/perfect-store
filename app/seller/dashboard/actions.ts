'use server';
import {createClient}from'@/lib/supabase/server';import {getSupabaseAdmin}from'@/lib/supabase/admin';import {z}from'zod';import {revalidatePath}from'next/cache';
const productSchema=z.object({name:z.string().trim().min(2).max(160),slug:z.string().trim().min(2).max(180),sku:z.string().trim().max(80).optional(),brand:z.string().trim().max(80).optional(),categoryId:z.string().uuid().optional(),description:z.string().trim().max(5000).optional(),shortDescription:z.string().trim().max(300).optional(),price:z.coerce.number().min(0),compareAtPrice:z.coerce.number().min(0).optional(),quantity:z.coerce.number().int().min(0),imageUrl:z.string().url().optional()});
async function getSellerSession(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return{supabase,sellerData:null};const{data:sellerData}=await supabase.from('sellers').select('id,status').eq('owner_id',user.id).maybeSingle();return{supabase,sellerData};}
export async function createProduct(input:unknown){const p=productSchema.safeParse(input);if(!p.success)return{success:false,error:p.error.issues[0]?.message};const{supabase,sellerData}=await getSellerSession();if(!sellerData||sellerData.status!=='active')return{success:false,error:'Your seller account is not active'};const db=getSupabaseAdmin();const d=p.data;const{data:product,error}=await db.from('products').insert({seller_id:sellerData.id,name:d.name,slug:d.slug,sku:d.sku||null,brand:d.brand||null,category_id:d.categoryId||null,description:d.description||null,short_description:d.shortDescription||null,base_price:d.price,compare_at_price:d.compareAtPrice||null,status:'pending_review',currency:'GHS',is_featured:false,rating_average:0,review_count:0}).select('id').single();if(error||!product)return{success:false,error:error?.message||'Unable to create product'};const{error:invError}=await db.from('inventory').insert({product_id:product.id,quantity:d.quantity,reserved_quantity:0,low_stock_threshold:5});if(invError){await db.from('products').delete().eq('id',product.id);return{success:false,error:'Unable to create inventory'}}if(d.imageUrl){const{error:imageError}=await db.from('product_images').insert({product_id:product.id,url:d.imageUrl,alt_text:d.name,sort_order:0,is_primary:true});if(imageError)return{success:false,error:imageError.message}}revalidatePath('/seller/dashboard');revalidatePath('/products');return{success:true,id:product.id};}
export async function updateProduct(productId:string,input:unknown){const p=productSchema.safeParse(input);if(!p.success)return{success:false,error:p.error.issues[0]?.message};const{supabase,sellerData}=await getSellerSession();if(!sellerData||sellerData.status!=='active')return{success:false,error:'Seller account is not active'};const db=getSupabaseAdmin();const d=p.data;const{error}=await db.from('products').update({name:d.name,slug:d.slug,sku:d.sku||null,brand:d.brand||null,category_id:d.categoryId||null,description:d.description||null,short_description:d.shortDescription||null,base_price:d.price,compare_at_price:d.compareAtPrice||null,status:'pending_review'}).eq('id',productId).eq('seller_id',sellerData.id);if(error)return{success:false,error:error.message};const{data:inv}=await db.from('inventory').select('reserved_quantity').eq('product_id',productId).maybeSingle();const{error:inventoryError}=await db.from('inventory').upsert({product_id:productId,quantity:d.quantity,reserved_quantity:inv?.reserved_quantity??0,low_stock_threshold:5},{onConflict:'product_id'});if(inventoryError)return{success:false,error:inventoryError.message};if(d.imageUrl){const{error:imageError}=await db.from('product_images').upsert({product_id:productId,url:d.imageUrl,alt_text:d.name,sort_order:0,is_primary:true});if(imageError)return{success:false,error:imageError.message}}revalidatePath('/seller/dashboard');revalidatePath('/products');return{success:true};}
export async function deleteProduct(productId:string){const{supabase,sellerData}=await getSellerSession();if(!sellerData||sellerData.status!=='active')return{success:false,error:'Seller account is not active'};const{error}=await getSupabaseAdmin().from('products').delete().eq('id',productId).eq('seller_id',sellerData.id);if(error)return{success:false,error:error.message};revalidatePath('/seller/dashboard');revalidatePath('/products');return{success:true};}
export async function updateSellerStore(input:{storeName:string;description?:string;phone?:string;email?:string}){const{supabase,sellerData}=await getSellerSession();if(!sellerData)return{success:false,error:'Seller account not found'};const{error}=await getSupabaseAdmin().from('sellers').update({store_name:input.storeName.trim(),description:input.description?.trim()||null,phone:input.phone?.trim()||null,email:input.email?.trim()||null}).eq('id',sellerData.id);if(error)return{success:false,error:error.message};revalidatePath('/seller/dashboard');return{success:true};}
