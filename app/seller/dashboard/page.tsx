import Link from 'next/link';import {redirect} from 'next/navigation';import {createClient} from '@/lib/supabase/server';import {SellerProductManager} from '@/components/seller/SellerProductManager';
export default async function SellerDashboardPage(){const s=await createClient();const{data:claims}=await s.auth.getClaims();const uid=typeof claims?.claims?.sub==='string'?claims.claims.sub:null;if(!uid)redirect('/login');const{data:p}=await s.from('profiles').select('role').eq('id',uid).single();if(p?.role!=='seller')redirect('/');const{data:seller}=await s.from('sellers').select('id,store_name,status,commission_rate').eq('owner_id',uid).maybeSingle();if(!seller||seller.status!=='active')redirect('/seller/apply');const[{data:products},{count:orders},{data:categories}]=await Promise.all([s.from('products').select('id,name,base_price,status,sku').eq('seller_id',seller.id).order('created_at',{ascending:false}),s.from('order_items').select('id',{count:'exact',head:true}).eq('seller_id',seller.id),s.from('categories').select('id,name').eq('is_active',true).order('sort_order')]);return <main className="min-h-screen bg-gray-50 px-4 py-8"><div className="mx-auto max-w-7xl"><div className="mb-8 flex items-end justify-between"><div><p className="text-sm font-medium text-blue-600">Seller Center</p><h1 className="text-3xl font-bold">{seller.store_name}</h1></div><div className="flex gap-4"><Link href="/seller/orders" className="text-sm font-medium text-blue-600">Orders</Link><Link href="/" className="text-sm font-medium text-blue-600">Storefront</Link></div></div><div className="mb-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border bg-white p-5"><p className="text-sm text-gray-500">Products</p><p className="mt-2 text-3xl font-bold">{products?.length??0}</p></div><div className="rounded-2xl border bg-white p-5"><p className="text-sm text-gray-500">Order items</p><p className="mt-2 text-3xl font-bold">{orders??0}</p></div><div className="rounded-2xl border bg-white p-5"><p className="text-sm text-gray-500">Commission</p><p className="mt-2 text-3xl font-bold">{seller.commission_rate}%</p></div></div><SellerProductManager categories={categories??[]} products={products??[]}/></div></main>}
