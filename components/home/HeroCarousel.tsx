'use client';
import React,{useState,useEffect,useCallback} from 'react';import Link from 'next/link';import Image from 'next/image';import {ChevronLeft,ChevronRight} from 'lucide-react';import {Button} from '@/components/ui/Button';import type {HeroSlide} from '@/lib/data/campaigns';
export function HeroCarousel({slides,autoPlayMs=5000}:{slides:HeroSlide[];autoPlayMs?:number}){const[current,setCurrent]=useState(0);const[paused,setPaused]=useState(false);const next=useCallback(()=>setCurrent(p=>(p+1)%slides.length),[slides.length]);const prev=()=>setCurrent(p=>(p-1+slides.length)%slides.length);useEffect(()=>{if(paused||slides.length<2)return;const t=setInterval(next,autoPlayMs);return()=>clearInterval(t)},[paused,next,autoPlayMs,slides.length]);const slide=slides[current];if(!slide)return null;return <div className="relative w-full overflow-hidden" style={{backgroundColor:slide.bgColor}} onMouseEnter={()=>setPaused(true)} onMouseLeave={()=>setPaused(false)}><div className="relative h-[280px] sm:h-[360px] md:h-[420px] lg:h-[480px]">{slide.imageUrl?<Image src={slide.imageUrl} alt={slide.headline} fill className="object-cover" sizes="100vw" priority={current===0}/>:<div className="absolute inset-0 opacity-20" style={{background:'radial-gradient(circle at 75% 35%, white 0, transparent 35%)'}}/>}<div className="absolute inset-0" style={{background:`linear-gradient(90deg,${slide.bgColor}ee 0%,${slide.bgColor}cc 45%,transparent 100%)`}}/><div className="absolute inset-0 flex items-center"><div className="mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8"><div className="max-w-xl"><h2 className="text-3xl font-bold leading-tight sm:text-4xl lg:text-5xl" style={{color:slide.textColor}}>{slide.headline}</h2><p className="mt-4 text-base sm:text-lg" style={{color:slide.textColor}}>{slide.subtext}</p><Link href={slide.ctaLink} className="mt-6 inline-block"><Button size="lg" className="bg-white font-semibold text-gray-900 hover:bg-gray-100">{slide.ctaText}</Button></Link></div></div></div></div>{slides.length>1&&<><button onClick={prev} aria-label="Previous slide" className="absolute left-4 top-1/2 flex h-10 w-10 -translate-y-1/2 items-center justify-center rounded-full bg-white/20 text-white backdrop-blur-sm"><ChevronLeft size={20}/></button><button onClick={next} aria-label="Next slide" className="absolute right-4 top-1/2 flex h-10 w-10 -translate-y-1/2 items-center justify-center rounded-full bg-white/20 text-white backdrop-blur-sm"><ChevronRight size={20}/></button></>}</div>}
