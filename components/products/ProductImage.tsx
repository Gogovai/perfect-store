import React from 'react';import Image from 'next/image';import Link from 'next/link';import {Package} from 'lucide-react';import type {ProductWithRelations} from '@/lib/queries/products';
interface ProductImageProps{product:ProductWithRelations;priority?:boolean;className?:string;sizes?:string}
export function ProductImage({product,priority=false,className='',sizes='(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 25vw'}:ProductImageProps){const primaryImage=product.product_images.find(img=>img.is_primary)||product.product_images[0];return <Link href={`/products/${product.slug}`} className={`relative block overflow-hidden bg-gray-50 ${className}`}>{primaryImage?<Image src={primaryImage.url} alt={primaryImage.alt_text||product.name} fill className="object-cover" sizes={sizes} priority={priority}/>:<div className="flex h-full min-h-32 flex-col items-center justify-center bg-gradient-to-br from-gray-50 to-gray-100 text-gray-300"><Package size={38} strokeWidth={1.4}/><span className="mt-2 text-xs text-gray-400">Product image pending</span></div>}</Link>}
